{
  "name": "etsy-social-pipeline",
  "version": "1.0.0",
  "description": "Big Little Pup Club — Etsy Social Media Multi-Agent Pipeline",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node index.ts",
    "pipeline": "ts-node index.ts",
    "pipeline:force": "FORCE_REFRESH=true ts-node index.ts",
    "publish": "ts-node index.ts --publish-only"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.36.0",
    "axios": "^1.7.9",
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.0"
  }
}
